[package]
name = "plugin-host"
version = "0.1.0"
edition = "2021"

# `plugin-api` and nothing else. The wasm runtime belongs to P8 of the
# plugin host plan; adding `wasmtime` here before there is code to use it
# would put a ~40 MB dependency tree and a cross-build risk into every
# build for no behaviour.
[dependencies]
plugin-api = { path = "../plugin-api" }

[dev-dependencies]
tempfile = "3"
