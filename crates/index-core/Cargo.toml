[package]
name = "index-core"
version = "0.1.0"
edition = "2021"

[dependencies]
tantivy = "0.22"
grep-searcher = "0.1"
grep-regex = "0.1"
grep-matcher = "0.1"
ignore = "0.4"

[dev-dependencies]
tempfile = "3"
