[package]
name = "app-core"
version = "0.1.0"
edition = "2021"

[dependencies]
editor-core = { path = "../editor-core" }
project-model = { path = "../project-model" }

[dev-dependencies]
tempfile = "3"
