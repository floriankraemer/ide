; Outline extraction query for Task D (`outline()`), YAML.
;
; A YAML document is nested data, not code: it has no classes, functions or
; fields in the sense a Class View shows. There is nothing meaningful to
; extract, so this file is intentionally empty (zero patterns, a valid and
; trivially-compiling tree-sitter query) — the same shape json/tags.scm uses.
