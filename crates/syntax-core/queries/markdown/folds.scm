; Foldable regions (Task C), Markdown: a section is everything under one
; heading — the fold a reader actually wants — plus code blocks, lists and
; block quotes.
(section) @fold
(fenced_code_block) @fold
(indented_code_block) @fold
(list) @fold
(block_quote) @fold
