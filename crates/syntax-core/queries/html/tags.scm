; Outline extraction query for Task D (`outline()`), HTML.
;
; A document tree is markup, not symbols: `SymbolKind` has no variant that
; an `<div>` maps onto without lying. Intentionally empty, the same shape
; yaml/tags.scm uses.
