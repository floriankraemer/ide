; Supertype-edge query (`supertype_edges()`), SQL — intentionally empty,
; the same shape json/inherits.scm uses. Relational schemas have no
; inheritance; a foreign key is a reference between tables, not a supertype
; relation, and is deliberately not reported.
