; Foldable regions (Task C): whole statements and the parenthesised bodies
; that carry their own brackets (subqueries, column definition lists).
(statement) @fold
(subquery) @fold
(column_definitions) @fold
