; Supertype-edge query (`supertype_edges()`), C.
;
; Intentionally empty: C has no inheritance, so there is no supertype edge
; to extract. The file exists because `queries!` in registry.rs includes
; all five `.scm` files for every language; an empty query compiles and
; yields no matches, which is exactly right here.
