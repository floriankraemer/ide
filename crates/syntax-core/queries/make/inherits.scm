; Supertype-edge query (`supertype_edges()`), Make — intentionally empty,
; the same shape json/inherits.scm uses. A makefile declares no types at
; all; the prerequisite graph is a dependency relation between targets, not
; a supertype relation, and is deliberately not reported.
