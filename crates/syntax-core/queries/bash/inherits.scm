; Bash has no type hierarchy, so this query is intentionally empty — the
; same shape json/inherits.scm uses. `supertype_edges()` returns an empty
; vec for Bash without needing a `None` special case in the dispatcher.
