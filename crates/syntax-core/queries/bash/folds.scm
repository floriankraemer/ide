; Foldable regions (Task C): function bodies and the block constructs that
; carry their own terminator (`fi`, `done`, `esac`).
(function_definition) @fold
(if_statement) @fold
(case_statement) @fold
(do_group) @fold
