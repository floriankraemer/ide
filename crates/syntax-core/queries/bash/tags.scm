; Outline extraction query for Task D (`outline()`), Bash. Shell scripts
; have exactly one kind of named definition — the function — so that is
; all this extracts.
(function_definition name: (word) @name) @definition.function
