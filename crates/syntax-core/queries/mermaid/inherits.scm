; Mermaid has no type hierarchy — a class diagram *describes* one, but the
; grammar's `class_declaration` nodes are drawings, not types this engine
; could resolve a supertype through. Intentionally empty, the same shape
; markdown/inherits.scm uses.
