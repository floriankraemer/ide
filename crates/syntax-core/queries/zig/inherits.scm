; Supertype-edge query (`supertype_edges()`), Zig — intentionally empty,
; the same shape json/inherits.scm uses. Zig has no inheritance and no
; interfaces: polymorphism is done with comptime duck typing and explicit
; vtable structs, neither of which is a syntactic supertype declaration.
