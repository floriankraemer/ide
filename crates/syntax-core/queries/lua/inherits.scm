; Supertype-edge query (`supertype_edges()`), Lua — intentionally empty,
; the same shape json/inherits.scm uses. Lua has no type declarations at
; all; inheritance is a runtime `setmetatable` call, which carries no
; syntactic supertype edge to extract.
