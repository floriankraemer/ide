; Foldable regions (Task C), inline Markdown. The inline grammar only ever
; sees one run of prose at a time, injected by the block grammar; nothing
; inside it spans lines in a way that is worth folding. Intentionally
; empty.
