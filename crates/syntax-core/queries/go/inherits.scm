; Supertype-edge query (`supertype_edges()`), Go — intentionally empty,
; the same shape json/inherits.scm uses. Go has no inheritance: interface
; satisfaction is structural and never written down at the declaration
; site, so there is no syntactic edge to extract. Struct embedding is
; composition, not a supertype relation, and is deliberately not reported.
