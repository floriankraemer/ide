; Outline extraction query for Task D (`outline()`), XML.
;
; XML is data, not code: an element is not a symbol any `SymbolKind`
; variant describes. Intentionally empty, the same shape yaml/tags.scm
; uses.
