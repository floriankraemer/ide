; Foldable regions (Task C), Haskell: class and instance bodies, `do`
; blocks, `where` bindings and multi-constructor data declarations.
(class_declarations) @fold
(instance_declarations) @fold
(local_binds) @fold
(data_constructors) @fold
(do) @fold
