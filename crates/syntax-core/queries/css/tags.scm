; Outline extraction query for Task D (`outline()`), CSS.
;
; A stylesheet has no classes, functions or fields in the sense a Class
; View shows — a rule set is a selector plus declarations, not a named
; symbol. Intentionally empty, the same shape yaml/tags.scm uses.
