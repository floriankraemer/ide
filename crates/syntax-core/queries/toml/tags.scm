; Outline extraction query for Task D (`outline()`), TOML.
;
; A TOML document is nested data, not code — no classes, functions or
; fields in the sense a Class View shows. Intentionally empty (zero
; patterns, a valid and trivially-compiling tree-sitter query), the same
; shape json/tags.scm uses.
